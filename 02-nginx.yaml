- name: Installing and run ngnix
  hosts: web
  become: yes #root access
  tasks:
  - name: install ngnix
    ansible.builtin.dnf:
      name: ngnix
      state: latest
  - name: start nginx
    ansible.builtin.service:
      name: ngnix
      state: started
      enabled: yes